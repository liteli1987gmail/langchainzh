默认情况下，LangSmith 遵循 `uv`/`pip` 的 **不** 安装预发布版本，除非明确允许。如果您想使用预发布版本，有以下选项：

- 使用 `pyproject.toml`：添加 `allow-prereleases = true` 到您的 `[tool.uv]` section.
- 使用 `requirements.txt` or `setup.py`：您必须明确指定每个预发布依赖项，包括传递性依赖项。例如，如果您声明 `a==0.0.1a1` 和 `a` 依赖于 `b==0.0.1a1`，那么您还必须明确包含 `b==0.0.1a1` 在您的依赖项中。
