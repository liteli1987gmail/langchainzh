---
title: "(MODULE_NAME) 集成"
description: "使用 LangChain Python 与 (MODULE_NAME) 中间件集成。"
---

如何使用此 Python 中间件模板：

- [ ] 将 (MODULE_NAME) 替换为中间件包或主类的名称（`command + F` 是你的好帮手）
- [ ] 更新链接以指向正确的中间件
- [ ] 在详情表下，替换占位符类名和链接；如果您的包导出多个主要中间件类，请添加行
- [ ] 在功能下，列出中间件的功能（钩子、模型配对、工具等）
- [ ] 如有需要，更新 PyPI 或注册表包名
- [ ] 如果中间件不使用 API 密钥，请更新或删除凭据部分

模板从这行以下开始...

本指南提供了开始使用 (MODULE_NAME) [中间件](/oss/langchain/middleware/overview/) 的快速概览。若需了解所有 (MODULE_NAME) 功能、参数和配置的详细列表，请前往 [(MODULE_NAME) API 参考](<LINK>)。

## 概述

### 详情

| 类 | 包 | 可序列化 | 下载量 | 版本 |
| :--- | :--- | :---: | :---: | :---: |
| [(`MODULE_NAME`)](https://reference.langchain.com/python/integrations/overview) | [`langchain-(MODULE_NAME)`](https://reference.langchain.com/python/integrations/overview) | beta/❌ | ![PyPI - Downloads](https://img.shields.io/pypi/dm/langchain-(MODULE_NAME)?style=flat-square&label=%20) | ![PyPI - Version](https://img.shields.io/pypi/v/langchain-(MODULE_NAME)?style=flat-square&label=%20) |

### 功能

- 在此列出功能（例如，提示缓存、提供商特定工具、防护栏）。

---

## 设置

要使用 (MODULE_NAME) 中间件，您需要创建一个 (MODULE_NAME) 账户，获取 API 密钥，并安装 `langchain-(MODULE_NAME)` 集成包。

### 凭据

```python Set API key icon="key"
import getpass
import os

if "(MODULE_NAME)_API_KEY" not in os.environ:
    os.environ["(MODULE_NAME)_API_KEY"] = getpass.getpass("Enter your (MODULE_NAME) API key: ")
```

设置 LangSmith 以实现可观测性也很有帮助（但非必需），并设置 <Tooltip tip="Log each step of a model's execution to debug and improve it">tracing</Tooltip>。要启用自动追踪功能，请设置您的 [LangSmith](/langsmith/observability) API 密钥：

```python Enable tracing icon="flask"
os.environ["LANGSMITH_API_KEY"] = getpass.getpass("Enter your LangSmith API key: ")
os.environ["LANGSMITH_TRACING"] = "true"
```

### 安装

(MODULE_NAME) 中间件位于 `langchain-(MODULE_NAME)` 包中：

<CodeGroup>
    ```python pip
    pip install -U langchain-(MODULE_NAME)
    ```
    ```python uv
    uv add langchain-(MODULE_NAME)
    ```
</CodeGroup>

---

## 实例化

导入并配置您的中间件。如果您的类位于子模块中，请调整导入路径（例如，`langchain_(MODULE_NAME).middleware.prompt_caching`）。

```python Initialize middleware icon="arrows-shuffle"
from langchain_(MODULE_NAME).middleware import (MODULE_NAME)

middleware = (MODULE_NAME)(...)
```

---

## 与代理一起使用

按顺序将中间件传递给 @[`create_agent`] 或 @[`create_deep_agent`]。你可以将提供者中间件与[内置中间件](/oss/langchain/middleware/built-in)结合使用。

```python Agent with middleware icon="robot"
from langchain.agents import create_agent

agent = create_agent(
    model="claude-sonnet-4-6",
    tools=[...],
    middleware=[middleware],
)

agent.invoke({"messages": [{"role": "user", "content": "INSERT QUERY"}]})
```

<Tip>
    中间件在[智能体循环](/oss/langchain/middleware/overview#the-agent-loop)中运行。如需自定义行为，请参阅[自定义中间件](/oss/langchain/middleware/custom)。
</Tip>

---

## TODO: 此中间件特有的功能

如不适用，请删除。

如果您公开多个中间件类，请在介绍后添加一个简短的概述表（参见 [Anthropic 中间件](/oss/integrations/middleware/anthropic)），并为每个中间件提供单独的章节，包含 API 参考、代码示例、配置选项和完整示例。

请参考现有中间件文档的示例：

- [Anthropic](/oss/integrations/middleware/anthropic)
- [AWS](/oss/integrations/middleware/aws)
- [OpenAI](/oss/integrations/middleware/openai)

示例：

- <Icon icon="database" size={16}/> 提示词或上下文缓存
- <Icon icon="terminal" size={16}/> 提供商原生工具与执行策略
- <Icon icon="shield-lock" size={16}/> 内容审核或安全护栏
- <Icon icon="user-check" size={16}/> 人工介入或审批流程

---

## API 参考

有关所有 (MODULE_NAME) 功能和配置的详细文档，请访问 [API 参考](<LINK>)。
