---
title: "AWS 中间件集成"
description: "使用 LangChain JavaScript 与 AWS 中间件集成。"
---

专门为托管在 AWS Bedrock 上的模型设计的中间件。了解更多关于[中间件](/oss/langchain/middleware/overview)的信息。

| 中间件 | 描述 |
|------------|-------------|
| [提示词缓存](#提示词缓存) | 通过缓存重复的提示词前缀来降低成本 |

## 提示词缓存

通过在 Amazon Bedrock 上缓存频繁重用的提示词前缀来减少推理延迟和输入 token 成本。`bedrockPromptCachingMiddleware` 通过 `modelSettings` 启用缓存。然后 `ChatBedrockConverse` 在请求时将其转换为正确的 AWS 线格式。缓存检查点被放置在系统提示词、工具定义以及最近的支持消息之后，以便模型可以在后续请求中跳过先前已看到内容的重新计算。缓存放置因模型系列而异：例如，Nova 跳过某些工具定义和工具结果案例。

提示词缓存在以下场景中很有用：

- 具有长且一致的系统提示词的多轮对话
- 具有许多在调用中保持不变的工具定义的代理
- 基于文档的问答，用户在同一上传上下文中提出多个问题
- 具有重复静态内容的批处理工作负载

支持的模型：

- **Anthropic Claude**
- **Amazon Nova**

<Info>
    了解更多关于 [AWS Bedrock 提示词缓存](https://docs.aws.amazon.com/bedrock/latest/userguide/prompt-caching.html) 策略和限制。缓存内容必须超过 1,024 个 token 才能使缓存检查点生效，有时根据模型可能需要更多。查看[支持的模型、区域和限制](https://docs.aws.amazon.com/bedrock/latest/userguide/prompt-caching.html#prompt-caching-models)。
</Info>

**API 参考：** @[`BedrockPromptCachingMiddleware`]

```typescript Model string
import { createAgent, bedrockPromptCachingMiddleware } from "langchain";

const agent = createAgent({
  model: "bedrock:us.anthropic.claude-sonnet-4-5-20250929-v1:0",
  systemPrompt: "<Your long system prompt here>",
  middleware: [bedrockPromptCachingMiddleware({ ttl: "1h" })], // [!code highlight]
});
```

```typescript ChatBedrockConverse instance
import { createAgent, bedrockPromptCachingMiddleware } from "langchain";
import { ChatBedrockConverse } from "@langchain/aws";

const agent = createAgent({
  model: new ChatBedrockConverse({ model: "us.anthropic.claude-sonnet-4-5-20250929-v1:0" }),
  systemPrompt: "<Your long system prompt here>",
  middleware: [bedrockPromptCachingMiddleware({ ttl: "5m" })], // [!code highlight]
});
```

<Accordion title="配置选项">

<ParamField body="enableCaching" type="boolean" default="true">
    是否应用提示词缓存。
</ParamField>

<ParamField body="ttl" type="string" default="5m">
    缓存内容的生存时间。有效值：`'5m'` 或 `'1h'`。请注意，Amazon Nova 模型仅支持 `'5m'`。
</ParamField>

<ParamField body="minMessagesToCache" type="number" default="1">
    开始缓存前的最小消息数。系统提示词算作一条消息。
</ParamField>

<ParamField body="unsupportedModelBehavior" type="string" default="warn">
    使用不支持的模型时的行为。选项：`'ignore'`、`'warn'` 或 `'raise'`。
</ParamField>

</Accordion>

<Accordion title="完整示例">

中间件会缓存每个请求中包含的最新消息及其之前的内容。在 TTL 窗口（5 分钟或 1 小时）内的后续请求中，之前见过的内容会从缓存中检索，而非重新处理，从而降低成本和延迟。

**工作原理：**
1. 首次请求：系统提示词、工具和用户消息会发送到 API 并被缓存
2. 第二次请求：从缓存中检索已缓存的内容。只需处理新消息
3. 每个轮次都会重复此模式，每个请求都会复用已缓存的对话历史

<Note>
    提示词缓存通过缓存 token 来降低 API 成本，但**不会**提供对话记忆功能。如需在调用之间持久化对话历史，请使用 [检查点器](/oss/langgraph/persistence#checkpointer-libraries)（如 `MemorySaver`）。
</Note>

</Accordion>

```typescript
import { createAgent, bedrockPromptCachingMiddleware, AIMessage, HumanMessage, tool } from "langchain";
import { ChatBedrockConverse } from "@langchain/aws";
import { z } from "zod";

const getWeather = tool(
  async ({ city }) => `The weather in ${city} is sunny and 72F.`,
  {
    name: "get_weather",
    description: "Get the current weather for a city.",
    schema: z.object({ city: z.string() }),
  }
);

// System prompt must exceed 1,024 tokens for caching to take effect
const LONG_PROMPT =
  "You are a helpful weather assistant with deep expertise in meteorology, " +
  "climate science, and atmospheric phenomena. When answering questions about " +
  "weather, provide accurate and up-to-date information. " +
  "You should always strive to give the most helpful response possible. ".repeat(85);

const agent = createAgent({
  model: new ChatBedrockConverse({ model: "us.anthropic.claude-sonnet-4-5-20250929-v1:0" }),
  systemPrompt: LONG_PROMPT,
  tools: [getWeather],
  middleware: [bedrockPromptCachingMiddleware({ ttl: "5m" })], // [!code highlight]
});

// First invocation: writes the cache (system prompt, tool definitions, and message)
let response = await agent.invoke({
  messages: [new HumanMessage("What is the weather in Miami?")],
});
const last = response.messages.at(-1);
console.log(last?.content);

// Check cache token usage
if (AIMessage.isInstance(last)) {
  const details = last.usage_metadata?.input_token_details;
  if (details) {
    console.log(`Cache read: ${details.cache_read ?? 0}, Cache write: ${details.cache_creation ?? 0}`);
  }
}

// Second invocation within the TTL: reuses the cached system prompt and tool definitions
response = await agent.invoke({
  messages: [new HumanMessage("How about Seattle?")],
});
console.log(response.messages.at(-1)?.content);
```

</Accordion>

### 特定模型行为

中间件自动处理不同模型系列之间的差异：

| 功能 | ChatBedrockConverse (Anthropic) | ChatBedrockConverse (Nova) |
|---------|:-------------------------------:|:--------------------------:|
| 系统提示词缓存 | ✅ | ✅ |
| 工具定义缓存 | ✅ | ❌ |
| 消息缓存 | ✅ | ✅（排除工具结果消息） |
| 延长 TTL（`1h`） | ✅ | ❌ |
