---
title: "Hugging Face 上的 Instructor 嵌入集成"
description: "使用 LangChain Python 与 Hugging Face 上的 Instructor 风格嵌入模型集成。"
---

import LangchainCommunityUnmaintained from '/snippets/oss/langchain-community-unmaintained.mdx';

>这些 `hkunlp/instructor-*` 系列引入了指令调优的句子嵌入。它们已被现代指令感知模型广泛超越，但原始模型仍在 Hugging Face 上可用，可通过旧版方式使用 `HuggingFaceInstructEmbeddings` class.

对于新项目，建议使用 `HuggingFaceEmbeddings` 从 [`langchain-huggingface`](/oss/integrations/embeddings/sentence_transformers) 配合当前的指令感知模型，例如 `intfloat/e5-large-v2`, `Qwen/Qwen3-Embedding-0.6B`, or `BAAI/bge-m3`。通过以下方式传递查询和文档提示 `encode_kwargs` 和 `query_encode_kwargs`:

```python
from langchain_huggingface import HuggingFaceEmbeddings

embeddings = HuggingFaceEmbeddings(
    model_name="intfloat/e5-large-v2",
    encode_kwargs={"prompt": "passage: "},
    query_encode_kwargs={"prompt": "query: "},
)
```

## 旧版 Instructor 用法

```shell
pip install -qU langchain-community InstructorEmbedding sentence-transformers
```

<LangchainCommunityUnmaintained />

```python
from langchain_community.embeddings import HuggingFaceInstructEmbeddings

embeddings = HuggingFaceInstructEmbeddings(
    query_instruction="Represent the query for retrieval: "
)

query_result = embeddings.embed_query("This is a test document.")
```
