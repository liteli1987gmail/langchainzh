---
name: write-timestamp
description: 当用户想要通过沙箱内打包的脚本将当前日期和时间写入文件时使用。
---

# 写入时间戳

## 说明

1. 脚本同步后在沙箱中的 `/skills/write-timestamp/write_timestamp.py` 路径可用。
2. 使用 `execute` 工具运行，例如：`python /skills/write-timestamp/write_timestamp.py`
3. 使用 `read_file` 读取 `/tmp/skill_timestamp.txt`，并将结果总结给用户。
