---
title: "DaytonaSandbox 集成"
description: "使用 LangChain Python 与 DaytonaSandbox 沙箱后端集成。"
---

[Daytona](https://daytona.io) 提供快速启动的沙箱环境，支持多种语言。注册、身份验证和平台详情请参阅 [Daytona 文档](https://www.daytona.io/docs)。

## 安装

<CodeGroup>

</CodeGroup>
```bash pip
pip install langchain-daytona
```

```bash uv
uv add langchain-daytona
```
</CodeGroup>

## 创建沙箱后端

在 Python 中，您使用提供商 SDK 创建沙箱，然后使用 [deepagents 后端](/oss/deepagents/backends) 包装它。

```python
from daytona import Daytona

from langchain_daytona import DaytonaSandbox

sandbox = Daytona().create()
backend = DaytonaSandbox(sandbox=sandbox)

result = backend.execute("echo hello")
print(result.output)
```

## 与 Deep Agents 配合使用

```python
from daytona import Daytona
from langchain_anthropic import ChatAnthropic

from deepagents import create_deep_agent
from langchain_daytona import DaytonaSandbox

sandbox = Daytona().create()
backend = DaytonaSandbox(sandbox=sandbox)

agent = create_deep_agent(
    model=ChatAnthropic(model="claude-sonnet-4-20250514"),
    system_prompt="You are a coding assistant with sandbox access.",
    backend=backend,
)

result = agent.invoke(
    {
        "messages": [
            {"role": "user", "content": "Create a hello world Python script and run it"}
        ]
    }
)
```

## 清理

你负责通过 Daytona SDK 管理沙箱的生命周期。
使用完毕后，请停止或销毁沙箱。

另请参阅：[沙箱](/oss/deepagents/sandboxes)。
