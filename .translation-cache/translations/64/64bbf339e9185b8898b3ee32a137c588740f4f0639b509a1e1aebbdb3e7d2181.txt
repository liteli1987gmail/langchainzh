---
title: 自定义流通道
description: 将自定义服务端数据流式传输到前端，并使用 useExtension 和 useChannel 进行读取
---

LangGraph 智能体流式传输的不仅是消息和工具调用。服务端**流转换器**可以在协议流向客户端时对其进行检查或重写，并在指定的**自定义通道**上发布自己的结构化数据。前端通过两个选择器读取该通道：@[`useExtension`] 用于获取最新载荷，@[`useChannel`] 作为原始事件的逃生通道。

以下示例是一个客户服务智能体，其转换器会对 PII（电子邮件、电话号码、社保号、银行卡号、IP 地址）在每个事件到达浏览器之前进行脱敏处理，并在一个 `redaction-stats` 通道上持续发布脱敏统计数据。侧边栏实时渲染这些统计数据。

import { PatternEmbed } from "/snippets/pattern-embed.jsx"
import UseStreamTypeInference from '/snippets/oss/use-stream-type-inference.mdx';

<PatternEmbed pattern="custom-stream-channel" />

## 自定义通道的工作原理

自定义通道有两个端点。在服务器端，一个 @[`StreamTransformer`] 打开一个命名的 @[`StreamChannel`] 并向其推送数据。在客户端，一个选择器订阅匹配的 `custom:<name>` 通道，并将数据作为响应式状态暴露。

转换器的 `process` 方法对每个协议事件运行。它可以就地修改事件（此处，从 `messages`、`tools` 和 `values` 数据中清除 PII），并在有内容需要报告时推送侧通道更新。

客户端选择器（`useExtension`、`useChannel`）随 v1 前端 SDK 包一起提供（`@langchain/react`、`@langchain/vue`、`@langchain/svelte`、`@langchain/angular`）。

:::python

<Note>
流转换器和 `StreamChannel` 需要 `langgraph>=1.2`。
</Note>

:::

:::js

<Note>
流转换器和 `StreamChannel` 需要 `@langchain/langgraph>=1.3.1`。
</Note>

:::

:::python

</fragment>

```python
import time

from langgraph.stream import ProtocolEvent, StreamChannel, StreamTransformer


class RedactionStatsTransformer(StreamTransformer):
    def __init__(self, scope: tuple[str, ...] = ()) -> None:
        super().__init__(scope)
        # Open a channel named "redaction-stats".
        self.redaction_stats = StreamChannel("redaction-stats")
        self.counts = empty_counts()

    def init(self) -> dict[str, StreamChannel]:
        return {"redactionStats": self.redaction_stats}

    def process(self, event: ProtocolEvent) -> bool:
        # Redact event["params"]["data"] in place and tally what was found.
        delta = redact_in_place(event, self.counts)
        if delta:
            # Publish a payload on the channel.
            self.redaction_stats.push(
                {
                    "kind": "update",
                    "at": int(time.time() * 1000),
                    "delta": delta,
                    "counts": dict(self.counts),
                    "total": sum(self.counts.values()),
                }
            )
        return True  # Keep the (now-redacted) event in the stream.


def create_redaction_stats_transformer() -> RedactionStatsTransformer:
    return RedactionStatsTransformer()
```

在构建代理时附加 transformer：

```python
from langchain.agents import create_agent

agent = create_agent(
    model="anthropic:claude-haiku-4-5",
    tools=[...],
    transformers=[create_redaction_stats_transformer],
)
```

:::

:::js

```ts
import { StreamChannel } from "@langchain/langgraph";
import type { ProtocolEvent, StreamTransformer } from "@langchain/langgraph";

export const createRedactionStatsTransformer = (): StreamTransformer<{
  redactionStats: StreamChannel<RedactionStatsEvent>;
}> => {
  // Open a remote channel named "redaction-stats".
  const redactionStats = StreamChannel.remote<RedactionStatsEvent>("redaction-stats");
  const counts = emptyCounts();

  return {
    init: () => ({ redactionStats }),

    process(event: ProtocolEvent): boolean {
      // Redact event.params.data in place and tally what was found.
      const delta = redactInPlace(event, counts);
      if (Object.keys(delta).length > 0) {
        // Publish a payload on the channel.
        redactionStats.push({
          kind: "update",
          at: Date.now(),
          delta,
          counts: { ...counts },
          total: totalRedactions(counts),
        });
      }
      return true; // Keep the (now-redacted) event in the stream.
    },
  };
};
```

在构建代理时附加转换器：

```ts
import { createAgent } from "langchain";

const agent = createAgent({
  model: "anthropic:claude-haiku-4-5",
  tools: [...],
  streamTransformers: [createRedactionStatsTransformer],
});
```

:::

有效载荷类型取决于 transformer 推送的内容。下面的客户端示例
读取此结构：

:::

```ts
type PiiType = "email" | "phone" | "ssn" | "credit_card" | "ip_address";

type RedactionStatsEvent = {
  kind: "update";
  at: number;
  delta: Partial<Record<PiiType, number>>;
  counts: Record<PiiType, number>;
  total: number;
};
```

## Setting up `useStream`

将 @[`useStream`] 接入使用。custom-channel 选择器接受这里返回的相同 `stream` 句柄。

<UseStreamTypeInference />

<CodeGroup>
```tsx React
import { useStream } from "@langchain/react";

const AGENT_URL = "http://localhost:2024";

export function RedactionChat() {
  const stream = useStream<typeof myAgent>({
    apiUrl: AGENT_URL,
    assistantId: "custom_stream_channel",
  });

  return <RedactionStatsPanel stream={stream} />;
}
```

```vue Vue
<script setup lang="ts">
import { useStream } from "@langchain/vue";

const AGENT_URL = "http://localhost:2024";

const stream = useStream<typeof myAgent>({
  apiUrl: AGENT_URL,
  assistantId: "custom_stream_channel",
});
</script>

<template>
  <RedactionStatsPanel :stream="stream" />
</template>
```

```svelte Svelte
<script lang="ts">
  import { useStream } from "@langchain/svelte";

  const AGENT_URL = "http://localhost:2024";

  const stream = useStream<typeof myAgent>({
    apiUrl: AGENT_URL,
    assistantId: "custom_stream_channel",
  });
</script>

<RedactionStatsPanel {stream} />
```

```ts Angular
import { Component } from "@angular/core";
import { injectStream } from "@langchain/angular";

const AGENT_URL = "http://localhost:2024";

@Component({
  selector: "app-redaction-chat",
  template: `<app-redaction-stats-panel [stream]="stream" />`,
})
export class RedactionChatComponent {
  stream = injectStream<typeof myAgent>({
    apiUrl: AGENT_URL,
    assistantId: "custom_stream_channel",
  });
}
```
</CodeGroup>

## 使用 `useExtension` 读取最新负载

`useExtension` 订阅 `custom:<name>` 频道并返回转换器推送的最新负载，已解包并已类型化。当 UI 只需要当前值时，这是符合人体工程学的选择，例如实时计数器、进度百分比或状态徽章。

传入裸频道名称（`"redaction-stats"`），而不是 `custom:` 前缀：

<CodeGroup>
```tsx React
import { useExtension } from "@langchain/react";

const latest = useExtension<RedactionStatsEvent>(stream, "redaction-stats");
// latest?.total, latest?.counts.email, latest?.delta
```

```vue Vue
import { useExtension } from "@langchain/vue";

const latest = useExtension<RedactionStatsEvent>(stream, "redaction-stats");
// latest.value?.total
```

```svelte Svelte
import { useExtension } from "@langchain/svelte";

const latest = useExtension<RedactionStatsEvent>(stream, "redaction-stats");
// latest?.total
```

```ts Angular
import { injectExtension } from "@langchain/angular";

const latest = injectExtension<RedactionStatsEvent>(stream, "redaction-stats");
// latest()?.total
```
</CodeGroup>

返回值遵循各框架的响应式模型：React 和 Svelte 返回普通值，Vue 返回 `Ref`（`latest.value`），Angular 返回 signal（`latest()`）。在第一个负载到来之前，值为 `undefined`。

可选的第三个 `target` 参数将订阅限定在某个命名空间，与 `useMessages(stream, node)` 将消息限定在已发现图节点的方式相同。
请参阅[图执行](/oss/langgraph/frontend/graph-execution)了解命名空间定向。

## 使用 `useChannel` 缓冲原始事件

`useChannel` 是原始事件的逃生舱口。它订阅一个或多个通道，并返回底层协议事件的有限缓冲区，而不是单个未包装的值。当你需要历史记录而非最新值时使用它，例如事件日志或审计跟踪，或者当需要一个没有更高级别选择器覆盖的通道时使用它。

传递完整的通道 id（`"custom:redaction-stats"`）：

<CodeGroup>
```tsx React
import { useChannel } from "@langchain/react";

const rawEvents = useChannel(stream, ["custom:redaction-stats"]);
```

```vue Vue
import { useChannel } from "@langchain/vue";

const rawEvents = useChannel(stream, ["custom:redaction-stats"]);
// rawEvents.value
```

```svelte Svelte
import { useChannel } from "@langchain/svelte";

const rawEvents = useChannel(stream, ["custom:redaction-stats"]);
```

```ts Angular
import { injectChannel } from "@langchain/angular";

const rawEvents = injectChannel(stream, ["custom:redaction-stats"]);
// rawEvents()
```
</CodeGroup>

每个条目都是原始协议事件，因此有效载荷位于
`event.params.data`。请自行解包：

```ts
function parseRedactionStatsEvents(rawEvents: Event[]): RedactionStatsEvent[] {
  const out: RedactionStatsEvent[] = [];
  for (const event of rawEvents) {
    const data = event.params?.data;
    const payload = data?.payload ?? data;
    if (payload?.kind === "update") out.push(payload);
  }
  return out;
}
```

使用 `options` 参数控制缓冲区：

```ts
const rawEvents = useChannel(
  stream,
  ["custom:redaction-stats"],
  undefined, // target namespace
  { bufferSize: 200, replay: true },
);
```

| 选项 | 默认值 | 效果 |
| --- | --- | --- |
| `bufferSize` | `"default"` | 缓冲事件的最大数量。达到上限后旧事件会被丢弃。 |
| `replay` | `true` | 在选择器挂载时重放频道上已看过的事件，而非仅接收实时事件。 |

<Note>
对于常见场景，优先使用高级别选择器（`useExtension`、`useMessages`、`useToolCalls`、`useValues`）。它们返回类型化的、非封装的值，且仅跟踪你渲染的内容。仅在你需要原始事件流时才使用 `useChannel`。
</Note>

## 在 `useExtension` 和 `useChannel` 之间选择

两者读取相同的自定义频道，但在返回值上有所不同：

| | `useExtension` | `useChannel` |
| --- | --- | --- |
| **返回** | 最新负载（`T \| undefined`） | 原始事件的有限缓冲（`Event[]`） |
| **形状** | 非封装的类型化负载 | 原始协议事件；需自行解封 `event.params.data` |
| **订阅方式** | 频道名称（`"redaction-stats"`） | 完整频道 ID（`["custom:redaction-stats"]`） |
| **适用场景** | 你需要当前值 | 你需要历史记录、日志或多频道 |
| **选项** | — | `bufferSize`、`replay` |

一种常见模式是在同一频道上同时使用两者：`useExtension` 驱动实时摘要（当前总数），而 `useChannel` 为整个线程中每次更新的滚动事件日志提供支持。

## 使用场景

自定义频道适用于任何无法干净地映射到消息、工具调用或图状态的服务器端信号：

- **合规性和编辑统计**：被清除的 PII、被屏蔽的内容或策略命中的计数，如上例所示。
- **进度报告**：由长时间运行的工具发出的完成百分比或步骤标签。
- **实时指标**：运行期间累积的 token 使用量、延迟或成本。
- **来源和引用**：代理在充实答案时推送到侧边栏的检索文档。
- **领域事件**：后端希望呈现的任何结构化更新，而无需更改消息记录。

## 相关内容

- [概述](/oss/langgraph/frontend/overview) — LangGraph 前端流 API 和架构。
- [图执行](/oss/langgraph/frontend/graph-execution) — 多节点管道的命名空间作用域选择器。
