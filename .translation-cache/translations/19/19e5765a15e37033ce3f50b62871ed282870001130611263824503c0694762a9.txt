---
title: "Writer PDF 解析器集成"
description: "使用 LangChain Python 与 Writer PDF 解析器文档加载器集成。"
---

本指南提供快速入门概览，帮助您开始使用 WRITER `PDFParser` [文档加载器](/oss/integrations/document_loaders/)。

WRITER 的 [PDF 解析器](https://dev.writer.com/api-guides/api-reference/tool-api/pdf-parser#parse-pdf) 可以将 PDF 文档转换为文本或 Markdown 等其他格式。当您需要从 PDF 文件中提取和处理文本内容以进行进一步分析或集成到工作流程中时，这尤其有用。在 `langchain-writer` 中，我们将 WRITER 的 PDF 解析器作为 LangChain 文档解析器提供使用。

<Warning>
**弃用通知**：PDF 解析工具已弃用，将于 **2025 年 12 月 22 日** 移除。

**迁移路径**：我们计划推出一个预构建的 PDF 解析工具，用于聊天补全，将提供类似的功能。该工具将与其他预构建工具类似地工作。我们将在该替代方案可用时提供更多详细信息。
</Warning>

## 概述

### 集成详情

| 类                                                                                                                              | 包          | 本地 | 可序列化 | JS 支持 |                                        下载量                                         |                                        版本                                         |
|:-----------------------------------------------------------------------------------------------------------------------------------|:-----------------| :---: | :---: |:----------:|:------------------------------------------------------------------------------------------------:|:---------------------------------------------------------------------------------------------:|
| [`PDFParser`](https://github.com/writer/langchain-writer/blob/main/langchain_writer/pdf_parser.py#L55) | [`langchain-writer`](https://pypi.org/project/langchain-writer/) |      ❌       |                                       ❌                                       | ❌ | ![PyPI - Downloads](https://img.shields.io/pypi/dm/langchain-writer?style=flat-square&label=%20) | ![PyPI - Version](https://img.shields.io/pypi/v/langchain-writer?style=flat-square&label=%20) |

## 安装

`PDFParser` 在 `langchain-writer` 包中可用：

```python
pip install --quiet -U langchain-writer
```

### 凭证

注册 [WRITER AI Studio](https://app.writer.com/aistudio/signup?utm_campaign=devrel) 来生成 API 密钥（您可以按照此[快速入门](https://dev.writer.com/api-guides/quickstart)进行操作）。然后，设置 WRITER_API_KEY 环境变量：

```python
import getpass
import os

if not os.getenv("WRITER_API_KEY"):
    os.environ["WRITER_API_KEY"] = getpass.getpass("Enter your WRITER API key: ")
```

设置 [LangSmith](https://smith.langchain.com) 也很有帮助（但不是必需的），以获得一流的可观测性。如果您希望这样做，可以设置 `LANGSMITH_TRACING` 和 `LANGSMITH_API_KEY` 环境变量：

```python
os.environ["LANGSMITH_TRACING"] = "true"
# os.environ["LANGSMITH_API_KEY"] = getpass.getpass()
```

### 实例化

接下来，使用所需的输出格式实例化 WRITER PDF Parser：

```python
from langchain_writer.pdf_parser import PDFParser

parser = PDFParser(format="markdown")
```

## 使用方法

PDF Parser 有两种使用方式：同步或异步。无论哪种方式，PDF Parser 都会返回一个 @[`Document`] 对象列表，每个对象包含 PDF 文件中一页的解析内容。

### 同步使用

要同步调用 PDF Parser，请将 `Blob` 对象传递给 `parse` 方法，引用要解析的 PDF 文件：

```python
from langchain_core.documents.base import Blob

file = Blob.from_path("../example_data/layout-parser-paper.pdf")

parsed_pages = parser.parse(blob=file)
parsed_pages
```

### 异步用法

要异步调用 PDF Parser，请将引用您想要解析的 PDF 文件的 `Blob` 对象传递给 `aparse` 方法：

```python
parsed_pages_async = await parser.aparse(blob=file)
parsed_pages_async
```

---
## 其他资源

您可以在 [WRITER 文档](https://dev.writer.com/home) 中找到有关 WRITER 模型（包括成本、上下文窗口和支持的输入类型）以及工具的信息。
