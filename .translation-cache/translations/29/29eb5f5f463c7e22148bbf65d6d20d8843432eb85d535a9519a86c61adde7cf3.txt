<CodeGroup>

</CodeGroup>
    ```ts Google
    import { MemoryVectorStore } from "@langchain/classic/vectorstores/memory";
    import { OpenAIEmbeddings } from "@langchain/openai";
    
    const vectorStore = new MemoryVectorStore(
      new OpenAIEmbeddings({ model: "google-genai:gemini-3.5-flash" }),
    );
    await vectorStore.addDocuments(allSplits);
    ```

    ```ts OpenAI
    import { MemoryVectorStore } from "@langchain/classic/vectorstores/memory";
    import { OpenAIEmbeddings } from "@langchain/openai";
    
    const vectorStore = new MemoryVectorStore(
      new OpenAIEmbeddings({ model: "openai:gpt-5.4" }),
    );
    await vectorStore.addDocuments(allSplits);
    ```

    ```ts Anthropic
    import { MemoryVectorStore } from "@langchain/classic/vectorstores/memory";
    import { OpenAIEmbeddings } from "@langchain/openai";
    
    const vectorStore = new MemoryVectorStore(
      new OpenAIEmbeddings({ model: "anthropic:claude-sonnet-4-6" }),
    );
    await vectorStore.addDocuments(allSplits);
    ```

    ```ts OpenRouter
    import { MemoryVectorStore } from "@langchain/classic/vectorstores/memory";
    import { OpenAIEmbeddings } from "@langchain/openai";
    
    const vectorStore = new MemoryVectorStore(
      new OpenAIEmbeddings({ model: "openrouter:anthropic/claude-sonnet-4-6" }),
    );
    await vectorStore.addDocuments(allSplits);
    ```

    ```ts Fireworks
    import { MemoryVectorStore } from "@langchain/classic/vectorstores/memory";
    import { OpenAIEmbeddings } from "@langchain/openai";
    
    const vectorStore = new MemoryVectorStore(
      new OpenAIEmbeddings({ model: "fireworks:accounts/fireworks/models/qwen3p5-397b-a17b" }),
    );
    await vectorStore.addDocuments(allSplits);
    ```

    ```ts Baseten
    import { MemoryVectorStore } from "@langchain/classic/vectorstores/memory";
    import { OpenAIEmbeddings } from "@langchain/openai";
    
    const vectorStore = new MemoryVectorStore(
      new OpenAIEmbeddings({ model: "baseten:zai-org/GLM-5.2" }),
    );
    await vectorStore.addDocuments(allSplits);
    ```

    ```ts Ollama
    import { MemoryVectorStore } from "@langchain/classic/vectorstores/memory";
    import { OpenAIEmbeddings } from "@langchain/openai";
    
    const vectorStore = new MemoryVectorStore(
      new OpenAIEmbeddings({ model: "ollama:devstral-2" }),
    );
    await vectorStore.addDocuments(allSplits);
    ```
<CodeGroup>

</CodeGroup>
