---
title: "Tensorlake 集成"
description: "使用 LangChain Python 与 Tensorlake 集成。"
---

Tensorlake 是一个 AI 数据云平台，可将来自非结构化来源的数据可靠地转换为可供 AI 应用摄入的格式。

`langchain-tensorlake` 包在 [Tensorlake](https://tensorlake.ai) 和 [LangChain](https://langchain.com) 之间提供无缝集成，使您能够构建具有增强解析功能的高级文档处理代理，例如签名检测。

## Tensorlake 功能概览

Tensorlake 为您提供以下工具：
- **提取**：模式驱动的结构化数据提取，从文档中提取特定字段。
- **解析**：将文档转换为 markdown 以构建 RAG/知识图谱系统。
- **编排**：构建可编程的工作流，用于大规模摄入和丰富文档、文本、音频、视频等。

在 [docs.tensorlake.ai](https://docs.tensorlake.ai/introduction) 了解更多信息

---

## 安装

<CodeGroup>
```bash pip
pip install -U langchain-tensorlake
```

```bash uv
uv add langchain-tensorlake
```
</CodeGroup>

---

## 示例

阅读[完整教程](https://docs.tensorlake.ai/examples/tutorials/real-estate-agent-with-langgraph-cli)，了解如何使用 `langchain-tensorlake` 工具检测非结构化文档中的签名。

或查看此 [Colab notebook](https://colab.research.google.com/drive/1VRWIPCWYnjcRtQL864Bqm9CJ6g4EpRqs?usp=sharing) 以快速入门。

---
## 快速入门

### 1. 设置环境

您需要通过设置以下环境变量来配置 Tensorlake 和 OpenAI 的凭据：
```
export TENSORLAKE_API_KEY="your-tensorlake-api-key"
export OPENAI_API_KEY = "your-openai-api-key"
```

从 [Tensorlake Cloud Console](https://cloud.tensorlake.ai/) 获取您的 Tensorlake API 密钥。新用户可获得 100 免费积分。

### 2. 导入必要的包

```python
from langchain_tensorlake import document_markdown_tool
from langchain.agents import create_agent
import asyncio
import os
```

### 3. 构建签名检测 Agent

```python
async def main(question):
    # Create the agent with the Tensorlake tool
    agent = create_agent(
            model="gpt-5.4-mini",
            tools=[document_markdown_tool],
            prompt=(
                """
                I have a document that needs to be parsed. \n\nPlease parse this document and answer the question about it.
                """
            ),
            name="real-estate-agent",
        )

    # Run the agent
    result = await agent.ainvoke({"messages": [{"role": "user", "content": question}]})

    # Print the result
    print(result["messages"][-1].content)
```

*注意：* 我们强烈建议使用 `openai` 作为代理模型以确保代理设置正确的解析参数

### 4. 示例用法

```python
# Define the path to the document to be parsed
path = "path/to/your/document.pdf"

# Define the question to be asked and create the agent
question = f"What contextual information can you extract about the signatures in my document found at {path}?"

if __name__ == "__main__":
    asyncio.run(main(question))
```

## 需要帮助？

在 [Slack](https://join.slack.com/t/tensorlakecloud/shared_invite/zt-32fq4nmib-gO0OM5RIar3zLOBm~ZGqKg) 上联系我们，或直接在
[GitHub 上的包仓库](https://github.com/tensorlakeai/langchain-tensorlake) 上联系我们。
