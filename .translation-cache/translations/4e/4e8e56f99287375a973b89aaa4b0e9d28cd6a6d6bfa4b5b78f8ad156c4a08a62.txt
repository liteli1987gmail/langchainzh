---
title: 使用 SDK 查询线程
sidebarTitle: 查询线程
description: 以编程方式从 LangSmith 项目中获取和检查多轮对话线程。
tag: "SDK"
---

如果您正在构建对话式代理或任何多轮应用，LangSmith 会自动将您的[运行](/langsmith/run-data-format)分组到[_线程_](/langsmith/observability-concepts#threads)中。查询线程让您可以重放完整对话、审计跨会话的代理行为、构建对话长度和延迟分析，并馈送下游工作流（如微调和评估）。

SDK 暴露了两个用于处理线程的方法：

| 方法 | 使用场景 |
|--------|-------------|
| [`list_threads`](https://reference.langchain.com/python/langsmith/client/Client/list_threads) / [`listThreads`](https://reference.langchain.com/javascript/langsmith/client/Client/listThreads) | 您想浏览项目中的所有线程 |
| [`read_thread`](https://reference.langchain.com/python/langsmith/client/Client/read_thread) / [`readThread`](https://reference.langchain.com/javascript/langsmith/client/Client/readThread) | 您已知道线程 ID 并需要其运行记录 |

## 线程工作原理

您创建的每个运行都可在其元数据中携带一个 `thread_id`。LangSmith 使用它将运行分组到线程中。后端会在 `metadata` 中查找 `thread_id`（回退到 `session_id`）。

<Note>
我们推荐使用 **UUID v7** 线程 ID。UUIDv7 内嵌时间戳，可保留线程的正确时间顺序。LangSmith SDK 导出了 uuid7 辅助函数（Python v0.4.43+，JS v0.3.80+）：

- **Python**: `from langsmith import uuid7`
- **JS/TS**: `import { uuid7 } from 'langsmith'`
</Note>

如果您正在使用[跟踪集成](/langsmith/integrations)，请在运行元数据中传递 `thread_id`：

<CodeGroup>

```python Python
from langsmith import traceable, uuid7

THREAD_ID = str(uuid7())

@traceable(metadata={"thread_id": THREAD_ID})
def my_agent(user_message: str) -> str:
    ...
```

```typescript TypeScript
import { traceable } from "langsmith/traceable";
import { uuid7 } from "langsmith";

const THREAD_ID = uuid7();

const myAgent = traceable(
  async (userMessage: string) => {
    // ...
  },
  { metadata: { thread_id: THREAD_ID } }
);
```

</CodeGroup>

## 列出项目中的所有线程

`list_threads` / `listThreads` 获取项目中的所有线程并将其运行分组在一起。结果按最近的活动优先排序。

<CodeGroup>

```python Python
from langsmith import Client

client = Client()

threads = client.list_threads(project_name="my-project")

for thread in threads:
    print(thread["thread_id"])
    print(f"  {thread['count']} runs")
    print(f"  last active: {thread['max_start_time']}")
```

```typescript TypeScript
import { Client } from "langsmith";

const client = new Client();

const threads = await client.listThreads({ projectName: "my-project" });

for (const thread of threads) {
  console.log(thread.thread_id);
  console.log(`  ${thread.count} runs`);
  console.log(`  last active: ${thread.max_start_time}`);
}
```

</CodeGroup>

结果按最近活动时间排序：

```text Output
conv-abc123
  3 runs
  last active: 2026-02-25T10:05:42+00:00
conv-def456
  1 runs
  last active: 2026-02-25T09:30:00+00:00
```

### 参数

| 参数 | 类型 | 默认值 | 描述 |
|-----------|------|---------|-------------|
| `project_name` / `projectName` | `string` | — | 项目名称。如果未设置 `project_id`，则必须提供。 |
| `project_id` / `projectId` | `string` | — | 项目 ID。如果未设置 `project_name`，则必须提供。 |
| `limit` | `int` | all | 返回的最大线程数。 |
| `offset` | `int` | `0` | 要跳过的线程数（用于分页）。 |
| `filter` | `string` | — | 获取运行时应用的过滤表达式，使用 [LangSmith 追踪查询语法](/langsmith/trace-query-syntax)。 |
| `start_time` / `startTime` | `datetime` / `Date` | 1 day ago | 仅包含在此时间之后开始的运行。扩大此范围以显示更早的线程。 |

### 返回值

线程对象列表，每个包含：

| 字段 | 类型 | 描述 |
|-------|------|-------------|
| `thread_id` | `string` | 线程标识符。 |
| `runs` | `@[Run][]` | 此线程中的根运行，按时间顺序排序（最早在前）。 |
| `count` | `int` | 此线程中的运行数。 |
| `min_start_time` | `string \| null` | 最早运行的 ISO 时间戳。 |
| `max_start_time` | `string \| null` | 最新运行的 ISO 时间戳。 |

<Note>
`list_threads` 始终仅返回根运行。如果需要子运行（例如工具调用、子链），请改用 `read_thread`，它接受一个可以设置为 `false` 的 `is_root` / `isRoot` 参数。
</Note>

## 读取单个线程的运行

当您已知晓 `thread_id` 时，请使用 `read_thread` / `readThread`。它直接返回线程运行的迭代器，无需先获取所有线程。

<CodeGroup>

```python Python
from langsmith import Client

client = Client()

for run in client.read_thread(
    thread_id="conv-abc123",
    project_name="my-project",
):
    print(run.id, run.name, run.start_time)
```

```typescript TypeScript
import { Client } from "langsmith";

const client = new Client();

for await (const run of client.readThread({
  threadId: "conv-abc123",
  projectName: "my-project",
})) {
  console.log(run.id, run.name, run.start_time);
}
```

</CodeGroup>

与 `list_threads` 不同，此处的每个条目都是 `Run` 对象本身——没有分组包装器。默认情况下，Run 按时间正序返回。

```python Output
[
    Run(id=UUID("a1b2..."), name="my_agent", run_type="chain", status="success", start_time=datetime(2026, 2, 25, 10, 0, 0, tzinfo=utc), ...),
    Run(id=UUID("c3d4..."), name="my_agent", run_type="chain", status="success", start_time=datetime(2026, 2, 25, 10, 3, 11, tzinfo=utc), ...),
    Run(id=UUID("e5f6..."), name="my_agent", run_type="chain", status="error",   start_time=datetime(2026, 2, 25, 10, 5, 42, tzinfo=utc), ...),
]
```

### 参数

| 参数 | 类型 | 默认值 | 描述 |
|-----------|------|---------|-------------|
| `thread_id` / `threadId` | `string` | — | **必填。** 要查询的线程。 |
| `project_name` / `projectName` | `string` | — | 项目名称。如果未设置 `project_id`，则必填。 |
| `project_id` / `projectId` | `string \| string[]` | — | 项目 ID 或 ID 列表。如果未设置 `project_name`，则必填。 |
| `is_root` / `isRoot` | `bool` | `true` | 仅返回根运行。设为 `false` 可包含子运行。 |
| `limit` | `int` | all | 返回的最大运行数。 |
| `filter` | `string` | — | 附加过滤表达式（与线程过滤器组合使用）。 |
| `order` | `"asc" \| "desc"` | `"asc"` | 排序顺序。`"asc"` 按时间顺序返回（最旧的在前）。 |
| `select` | `string[]` | all fields | 要返回的特定运行字段，用于减少响应大小。 |

### 返回值

一个迭代器（[Python](https://reference.langchain.com/python/langsmith)）或异步迭代器（[TypeScript](https://reference.langchain.com/javascript/langsmith)），包含 `Run` 对象。

## 示例

### 按运行属性筛选线程

传递过滤表达式以使用 [LangSmith 追踪查询语法](/langsmith/trace-query-syntax)缩小结果范围。例如，仅显示包含至少一个失败运行的线程：

<CodeGroup>

</CodeGroup>

```python Python
threads = client.list_threads(
    project_name="my-project",
    filter='eq(status, "error")',
)
```

```typescript TypeScript
const threads = await client.listThreads({
  projectName: "my-project",
  filter: 'eq(status, "error")',
});
```

<CodeGroup>

### 回顾超过 24 小时前的数据

默认情况下，`list_threads` 只显示最近一天内有运行的线程。传入 `start_time` 来扩大查询时间范围：

<CodeGroup>

</fragment>

```python Python
import datetime

threads = client.list_threads(
    project_name="my-project",
    start_time=datetime.datetime.now(datetime.timezone.utc) - datetime.timedelta(days=2),
)
```

```typescript TypeScript
const threads = await client.listThreads({
  projectName: "my-project",
  startTime: new Date(Date.now() - 2 * 24 * 60 * 60 * 1000),
});
```

</CodeGroup>

### 重建对话

使用 `read_thread` 和 `order="asc"` 按顺序重放对话：

<CodeGroup>

```python Python
runs = list(
    client.read_thread(
        thread_id="conv-abc123",
        project_name="my-project",
        order="asc",
    )
)

for run in runs:
    user_msg = run.inputs.get("messages", [{}])[-1].get("content", "")
    assistant_msg = (run.outputs or {}).get("content", "")
    print(f"User:      {user_msg}")
    print(f"Assistant: {assistant_msg}")
    print()
```

```typescript TypeScript
const runs: Run[] = [];
for await (const run of client.readThread({
  threadId: "conv-abc123",
  projectName: "my-project",
  order: "asc",
})) {
  runs.push(run);
}

for (const run of runs) {
  const messages = (run.inputs?.messages ?? []) as Array<Record<string, string>>;
  const userMsg = messages.at(-1)?.content ?? "";
  const assistantMsg = (run.outputs as Record<string, string>)?.content ?? "";
  console.log(`User:      ${userMsg}`);
  console.log(`Assistant: ${assistantMsg}`);
}
```

<CodeGroup>

</CodeGroup>
