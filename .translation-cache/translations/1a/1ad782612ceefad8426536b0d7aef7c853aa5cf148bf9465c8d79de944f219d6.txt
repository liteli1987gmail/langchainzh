---
title: "InMemoryStore 集成"
description: "使用 LangChain JavaScript 与 InMemoryStore 存储集成。"
---

这将帮助您开始使用 [InMemoryStore](/oss/integrations/stores)。有关所有 `InMemoryStore` 功能和配置的详细文档，请参阅 [API 参考](https://reference.langchain.com/javascript/langchain-core/stores/InMemoryStore).

该 `InMemoryStore` 允许将泛型类型分配给存储中的值。我们将分配类型 `BaseMessage` 作为值的类型，延续聊天历史存储的主题。

## 概述

### 集成详情

| 类 | 包 | 本地 | [PY 支持](https://python.langchain.com/docs/integrations/stores/in_memory/) | 下载量 | 版本 |
| :--- | :--- | :---: | :---: |  :---: | :---: |
| [`InMemoryStore`](https://reference.langchain.com/javascript/langchain-core/stores/InMemoryStore) | [`@langchain/core`](https://www.npmjs.com/package/@langchain/core) | ✅ | ✅ | ![NPM - 下载量](https://img.shields.io/npm/dm/@langchain/core?style=flat-square&label=%20&) | ![NPM - 版本](https://img.shields.io/npm/v/@langchain/core?style=flat-square&label=%20&) |

## 设置

### 安装

LangChain InMemoryStore 集成位于 `@langchain/core` package:

<CodeGroup>
```bash npm
npm install @langchain/core
```
```bash yarn
yarn add @langchain/core
```
```bash pnpm
pnpm add @langchain/core
```
</CodeGroup>

## 实例化

现在我们可以实例化字节存储：

```typescript
import { InMemoryStore } from "@langchain/core/stores"
import { BaseMessage } from "@langchain/core/messages";

const kvStore = new InMemoryStore<BaseMessage>();
```

## 用法

您可以使用以下方式在键下设置数据 `mset` method:

```typescript
import { AIMessage, HumanMessage } from "@langchain/core/messages";

await kvStore.mset(
  [
    ["key1", new HumanMessage("value1")],
    ["key2", new AIMessage("value2")],
  ]
)

await kvStore.mget(
  [
    "key1",
    "key2",
  ]
)
```

```json
[
  HumanMessage {
    "content": "value1",
    "additional_kwargs": {},
    "response_metadata": {}
  },
  AIMessage {
    "content": "value2",
    "additional_kwargs": {},
    "response_metadata": {},
    "tool_calls": [],
    "invalid_tool_calls": []
  }
]
```

您可以使用以下方式删除数据 `mdelete` method:

```typescript
await kvStore.mdelete(
  [
    "key1",
    "key2",
  ]
)

await kvStore.mget(
  [
    "key1",
    "key2",
  ]
)
```

```text
[ undefined, undefined ]
```

## 返回值

如果您想获取所有键，可以调用 `yieldKeys` 方法。可选地，您可以传递键前缀以仅获取与该前缀匹配的键。

```typescript
import { InMemoryStore } from "@langchain/core/stores"
import { AIMessage, BaseMessage, HumanMessage } from "@langchain/core/messages";

const kvStoreForYield = new InMemoryStore<BaseMessage>();

// Add some data to the store
await kvStoreForYield.mset(
  [
    ["message:id:key1", new HumanMessage("value1")],
    ["message:id:key2", new AIMessage("value2")],
  ]
)

const yieldedKeys = [];
for await (const key of kvStoreForYield.yieldKeys("message:id:")) {
  yieldedKeys.push(key);
}

console.log(yieldedKeys);
```

```python
[ 'message:id:key1', 'message:id:key2' ]
```

---

## API 参考

有关所有 `InMemoryStore` 功能和配置的详细文档，请参阅 [API 参考](https://reference.langchain.com/javascript/langchain-core/stores/InMemoryStore)
