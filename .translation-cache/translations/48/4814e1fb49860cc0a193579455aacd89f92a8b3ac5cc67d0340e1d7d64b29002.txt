---
title: "(MODULE_NAME) 集成"
description: "使用 LangChain Python 与 (MODULE_NAME) 工具集成。"
---
---

如何使用此 Python 工具模板：

- [ ] 将 (MODULE_NAME) 替换为工具名称 (`command + F` 在此处可助你一臂之力)
- [ ] 更新链接以指向正确的工具
- [ ] Under the details and features tables, update the ✅/❌ to reflect the actual capabilities of the tool
- [ ] Update the PyPI/registry package name if needed
- [ ] 如有需要，更新 API 密钥环境变量名称

模板从此行开始...

本指南提供快速入门概述，帮助你开始使用 (MODULE_NAME) [工具](/oss/langchain/tools)。有关 (MODULE_NAME) 的完整功能、参数和配置详情，请参阅 [(MODULE_NAME) API 参考](<LINK>).

## 概述

### 详情

| 类 | 包 | 可序列化 | [JS 支持](https://js.langchain.com/docs/integrations/chat/anthropic) | 下载量 | 版本 |
| :--- | :--- | :---: | :---: |  :---: | :---: | :---: |
| [(MODULE_NAME)](<LINK>) | [langchain-(MODULE_NAME)](<LINK>) | beta/❌ | ✅/❌ | ![PyPI - 下载量](https://img.shields.io/pypi/dm/langchain-(MODULE_NAME)?style=flat-square&label=%20) | ![PyPI - 版本](https://img.shields.io/pypi/v/langchain-(MODULE_NAME)?style=flat-square&label=%20) |

### 功能

- 在此列出功能。如有需要，可添加图标！

---

## 设置

要访问 (MODULE_NAME) tool, you'll need to create a/an (MODULE_NAME) 账户，获取 API 密钥，并安装 `langchain-(MODULE_NAME)` 集成包。

### 凭证

```python Set API key icon="key"
import getpass
import os

if "(MODULE_NAME)_API_KEY" not in os.environ:
    os.environ["(MODULE_NAME)_API_KEY"] = getpass.getpass("Enter your (MODULE_NAME) API key: ")
```


It's also helpful (but not needed) to set up LangSmith for best-in-class observability/<Tooltip tip="Log each step of a model's execution to debug and improve it">追踪</Tooltip> 你的工具调用。要启用自动追踪，请设置你的 [LangSmith](/langsmith/observability) API 密钥：

```python Enable tracing icon="flask"
os.environ["LANGSMITH_API_KEY"] = getpass.getpass("Enter your LangSmith API key: ")
os.environ["LANGSMITH_TRACING"] = "true"
```

### 安装

(MODULE_NAME) 工具位于 `langchain-(MODULE_NAME)` package:

<CodeGroup>
    ```python pip
    pip install -U langchain-(MODULE_NAME)
    ```
    ```python uv
    uv add langchain-(MODULE_NAME)
    ```
</CodeGroup>

---

## 实例化

现在我们可以使用以下方式实例化 (MODULE_NAME) 工具：

```python Initialize tool instance icon="robot"
from langchain_(MODULE_NAME) import (MODULE_NAME)

tool = (MODULE_NAME)(...)
```

---

## 调用

### 直接调用

TODO：描述工具参数，填写具体内容

```python Call tool icon="rocket"
tool.invoke({...})
```

### 作为 ToolCall 使用

我们也可以使用模型生成的 `ToolCall`来调用工具，在这种情况下会返回一个 @[`ToolMessage`]：

```python ToolCall icon="briefcase"
# This is usually generated by a model, but we'll create a tool call directly for demo purposes.
model_generated_tool_call = {
    "args": {...},  # TODO: FILL IN
    "id": "1",
    "name": tool.name,
    "type": "tool_call",
}
tool.invoke(model_generated_tool_call)
```

### 在 Agent 中使用

我们可以在 Agent 中使用工具。为此，我们需要一个具有工具调用能力的大语言模型。

```python Agent with tool icon="robot"
# pip install -qU "langchain[anthropic]" to call the model
from langchain.agents import create_agent

tools = [tool]
agent = create_agent(
    model="claude-sonnet-4-6",
    tools=tools,
)

# Run the agent
agent.invoke(
    {"messages": [{"role": "user", "content": "INSERT QUERY"}]}
)
```

---

## TODO：此工具的任何特定功能

如不适用，请删除。

查看现有工具文档以获取示例，例如：

- [MCP 工具箱](/oss/integrations/tools/toolbox)
- [Discord](/oss/integrations/tools/discord)
- 

---

## API 参考文档

有关所有 (MODULE_NAME) 功能配置的详细文档，请访问 [API 参考文档](<LINK>).
