---
title: "(MODULE_NAME) integration"
description: "使用 LangChain Python 与 (MODULE_NAME) 工具集成。"
---

如何使用此 Python 工具模板：

- [ ] 将 (MODULE_NAME) 替换为工具名称（`command + F` 是你的好帮手）
- [ ] 更新链接以指向正确的工具
- [ ] 在详情和功能表格下，将 ✅/❌ 更新为反映工具实际功能的内容
- [ ] 如有需要，更新 PyPI/registry 包名称
- [ ] 如有需要，更新 API 密钥环境变量名称

模板从这行以下开始...

本指南提供了快速入门 (MODULE_NAME) [工具](/oss/langchain/tools) 的概览。有关 (MODULE_NAME) 所有功能、参数和配置的详细列表，请前往 [(MODULE_NAME) API 参考](<LINK>)。

## 概述

### 详情

| 类 | 包 | 可序列化 | [JS 支持](https://js.langchain.com/docs/integrations/chat/anthropic) | 下载量 | 版本 |
| :--- | :--- | :---: | :---: |  :---: | :---: |
| [(MODULE_NAME)](<LINK>) | [langchain-(MODULE_NAME)](<LINK>) | beta/❌ | ✅/❌ | ![PyPI - Downloads](https://img.shields.io/pypi/dm/langchain-(MODULE_NAME)?style=flat-square&label=%20) | ![PyPI - Version](https://img.shields.io/pypi/v/langchain-(MODULE_NAME)?style=flat-square&label=%20) |

### 功能

- 在此列出功能。如有需要，可添加图标！

---

## 设置

要访问 (MODULE_NAME) 工具，你需要创建一个 (MODULE_NAME) 账户，获取一个 API 密钥，并安装 `langchain-(MODULE_NAME)` 集成包。

### 凭证

```python Set API key icon="key"
import getpass
import os

if "(MODULE_NAME)_API_KEY" not in os.environ:
    os.environ["(MODULE_NAME)_API_KEY"] = getpass.getpass("Enter your (MODULE_NAME) API key: ")
```


设置 LangSmith 也是有帮助的（但非必需），以便获得一流的观测性/<Tooltip tip="Log each step of a model's execution to debug and improve it">追踪</Tooltip>工具调用。要启用自动追踪，请设置你的 [LangSmith](/langsmith/observability) API 密钥：

```python Enable tracing icon="flask"
os.environ["LANGSMITH_API_KEY"] = getpass.getpass("Enter your LangSmith API key: ")
os.environ["LANGSMITH_TRACING"] = "true"
```

### 安装

(MODULE_NAME) 工具位于 `langchain-(MODULE_NAME)` 包中：

<CodeGroup>

</CodeGroup>
    ```python pip
    pip install -U langchain-(MODULE_NAME)
    ```
    ```python uv
    uv add langchain-(MODULE_NAME)
    ```
</CodeGroup>

---

## 实例化

现在我们可以使用以下方式实例化 (MODULE_NAME) 工具：

```python Initialize tool instance icon="robot"
from langchain_(MODULE_NAME) import (MODULE_NAME)

tool = (MODULE_NAME)(...)
```

---

## 调用

### 直接

TODO: 描述工具参数的含义并进行填写

```python Call tool icon="rocket"
tool.invoke({...})
```

### 作为 ToolCall

我们也可以使用模型生成的 `ToolCall` 来调用该工具，在这种情况下将返回一个 @[`ToolMessage`]：

```python ToolCall icon="briefcase"
# This is usually generated by a model, but we'll create a tool call directly for demo purposes.
model_generated_tool_call = {
    "args": {...},  # TODO: FILL IN
    "id": "1",
    "name": tool.name,
    "type": "tool_call",
}
tool.invoke(model_generated_tool_call)
```

### 在智能体中

我们可以在智能体中使用我们的工具。为此，我们需要一个支持工具调用的模型。

```python Agent with tool icon="robot"
# pip install -qU "langchain[anthropic]" to call the model
from langchain.agents import create_agent

tools = [tool]
agent = create_agent(
    model="claude-sonnet-4-6",
    tools=tools,
)

# Run the agent
agent.invoke(
    {"messages": [{"role": "user", "content": "INSERT QUERY"}]}
)
```

---

## TODO: 特定于此工具的功能

如有需要，请保留；如不需要，请删除。

请参阅现有工具文档中的示例，例如：

- [MCP Toolbox](/oss/integrations/tools/toolbox)
- [Discord](/oss/integrations/tools/discord)
- 

---

## API 参考

有关所有 (MODULE_NAME) 功能和配置的详细文档，请前往 [API 参考](<LINK>)。
