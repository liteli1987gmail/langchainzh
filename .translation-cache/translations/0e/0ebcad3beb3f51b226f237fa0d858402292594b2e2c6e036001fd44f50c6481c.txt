---
title: 安装 Mission Control
sidebarTitle: Mission Control
description: 安装 Mission Control，这是一个集群内控制台，用于监控、配置和操作 Kubernetes 上自托管的 LangSmith。
---

Mission Control 是一个集群内控制台，用于监控、配置和操作 Kubernetes 上的 LangSmith。它在集群内部运行，默认通过 `kubectl port-forward` 访问，因此无需配置 ingress。

有两种安装路径：

| 路径 | 适用场景 |
|------|----------|
| [快速安装](#quick-install) | 可以运行已审核的 shell 安装脚本、希望设置过程最短的用户。 |
| [手动安装](#manual-install) | 不允许运行安装脚本或需要审核每条 Kubernetes 命令的组织。 |

公共安装资源包括：

- `install-script.sh`：一个安装程序，包含独立的 `prereqs`、`namespace`、`secret`、`values`、`install` 和 `forward` 步骤。
- `values.yaml`：仅使用 port-forward 方式安装的默认 Helm values 文件。

Mission Control 镜像发布到两个 Docker Hub 仓库：`langchain/mission-control-backend` 和 `langchain/mission-control-frontend`。可以使用以下命令检查最新镜像：

```bash
docker pull langchain/mission-control-backend:latest
docker pull langchain/mission-control-frontend:latest
```

浏览器查看链接：

- `https://github.com/langchain-ai/helm/tree/main/charts/mission-control/install-script.sh`
- `https://github.com/langchain-ai/helm/tree/main/charts/mission-control/values.yaml`

下面的命令使用原始 GitHub URL，以便 `curl` 可以直接下载文件。如果您从不同的仓库或分支发布这些文件，请替换下面的原始 base URL。

```bash
MC_RAW_BASE=https://raw.githubusercontent.com/langchain-ai/helm/main/charts/mission-control
```

## 先决条件

| 工具 | 最低版本 | 安装示例 |
|------|-----------------|-----------------|
| `kubectl` | 1.24+ | `brew install kubectl` |
| `helm` | 3.x | `brew install helm` |
| `curl` | 当前任何版本 | 通常预装 |

必须在已安装或将要安装 LangSmith 的 Kubernetes 集群上运行安装程序。继续之前，请确认当前上下文：

```bash
kubectl config current-context
```

安装程序身份需要权限来创建 Mission Control 命名空间资源和集群范围的 RBAC：

```bash
kubectl auth can-i create clusterrole
kubectl auth can-i create clusterrolebinding
kubectl auth can-i create serviceaccount -n langsmith
kubectl auth can-i create deployment -n langsmith
kubectl auth can-i create secret -n langsmith
```

所有五个命令都应该返回 `yes`。请参阅 [权限参考](#permissions-reference) 了解授予 Mission Control 的运行时权限。

## 快速安装

运行以下三个命令：

```bash
curl -fsSLO https://raw.githubusercontent.com/langchain-ai/helm/main/charts/mission-control/install-script.sh && chmod +x install-script.sh
curl -fsSL https://raw.githubusercontent.com/langchain-ai/helm/main/charts/mission-control/values.yaml -o values.yaml
./install-script.sh all -f values.yaml
```

`all` 步骤：

- 检查必需的工具和 RBAC。
- 创建 `langsmith` 命名空间。
- 提示输入 Mission Control 用户名和密码。
- 将这些凭证存储在 `mission-control-auth` Kubernetes Secret 中。
- 如果 `values.yaml` 不存在，则创建它。
- 如果您不是从本地 chart 副本运行，则从公共 Helm chart 仓库安装。
- 使用 Helm 安装 Mission Control。

RBAC 检查会运行：

```bash
kubectl auth can-i create clusterrole
kubectl auth can-i create clusterrolebinding
kubectl auth can-i create serviceaccount -n langsmith
kubectl auth can-i create deployment -n langsmith
kubectl auth can-i create secret -n langsmith
```

如果您的组织有意阻止 `kubectl auth can-i` 但通过其他控制路径批准了 Helm 安装，请运行：

```bash
./install-script.sh all -f values.yaml --skip-rbac-check
```

### 访问 UI

安装完成后，启动本地端口转发：

```bash
./install-script.sh forward
```

打开 [http://localhost:3000](http://localhost:3000/)，然后使用您输入的用户名和密码登录。

### 先检查脚本

快速安装路径会在运行脚本之前先下载脚本，因此您可以在执行第三个命令之前在本地检查 `install-script.sh`。

### 安装前编辑配置值

快速安装路径还会在运行安装程序之前下载 `values.yaml`。如果您需要更改命名空间、资源、入口、功能标志或诊断持久化，请在第三个命令之前检查或编辑该文件。

常见编辑项：

| 设置 | 何时修改 |
|---------|-------------------|
| `namespace` | 将 Mission Control 安装在 `langsmith` 以外的其他位置。同时请将 `-n <namespace>` 传递给脚本。 |
| `resources` | 您的命名空间有 `ResourceQuota`，或者您的平台对请求/限制有特定要求。 |
| `ingress.enabled` 和 `ingress.host` | 您希望通过入口控制器而不是端口转发来暴露 Mission Control。 |
| `config.features.*` | 您需要移除特定的写权限或外部出口功能。 |
| `diagnostics.persistence.enabled` | 您希望诊断包能够在 Pod 重启和 Helm 升级后保留。 |
| `backend.podSecurityContext` 和 `frontend.podSecurityContext` | 您的平台要求容器以特定非 root UID 运行，例如 `1001`。 |

使用自定义命名空间的示例：

```bash
curl -fsSLO https://raw.githubusercontent.com/langchain-ai/helm/main/charts/mission-control/install-script.sh && chmod +x install-script.sh
curl -fsSL https://raw.githubusercontent.com/langchain-ai/helm/main/charts/mission-control/values.yaml -o values.yaml
./install-script.sh all -n mission-control -f values.yaml
```

### 脚本命令参考

```bash
./install-script.sh prereqs
./install-script.sh namespace
./install-script.sh secret
./install-script.sh values
./install-script.sh install
./install-script.sh forward
./install-script.sh all
```

有用的参数：

```bash
./install-script.sh all -n langsmith -f values.yaml
./install-script.sh all -u admin
printf '%s\n' 'your-password' | ./install-script.sh secret -u admin --password-stdin
./install-script.sh all -f values.yaml --skip-rbac-check
./install-script.sh install --chart-ref langchain/mission-control
./install-script.sh install --chart-path /path/to/mission-control
./install-script.sh forward --port 3001:3000
```

## 手动安装

当不允许使用安装程序脚本时，请使用此路径。这些步骤仅使用普通的 `kubectl`、`helm` 和 `curl` 命令。

<Steps>
  <Step title="添加 Helm 仓库并获取 values 文件">
    添加 LangChain Helm 仓库：


</Steps>

    ```bash
    helm repo add langchain https://langchain-ai.github.io/helm
    helm repo update langchain
    ```

下载客户值文件：

    ```bash
    curl -fsSL https://raw.githubusercontent.com/langchain-ai/helm/main/charts/mission-control/values.yaml -o values.yaml
    ```

在安装前检查 `values.yaml`。生产环境中保持 `config.auth.enabled: true`。

如果您的平台需要非 root 容器，Mission Control 可以使用 UID `1001` 运行。在安装前将此添加到 `values.yaml`：

    ```yaml
    backend:
      podSecurityContext:
        runAsNonRoot: true
        runAsUser: 1001
        runAsGroup: 1001
        fsGroup: 1001
      securityContext:
        allowPrivilegeEscalation: false
        capabilities:
          drop:
            - ALL
      extraEnv:
        - name: HOME
          value: /tmp
        - name: HELM_CACHE_HOME
          value: /tmp/.cache/helm
        - name: HELM_CONFIG_HOME
          value: /tmp/.config/helm
        - name: HELM_DATA_HOME
          value: /tmp/.local/share/helm

    frontend:
      podSecurityContext:
        runAsNonRoot: true
        runAsUser: 1001
        runAsGroup: 1001
      securityContext:
        allowPrivilegeEscalation: false
        capabilities:
          drop:
            - ALL
    ```
</Step>
  <Step title="创建命名空间">
    ```bash
    kubectl create namespace langsmith --dry-run=client -o yaml | kubectl apply -f -
    ```
</Step>
  <Step title="创建认证凭证 Secret">
    凭证存储在 Kubernetes Secret 中，不会写入 `values.yaml`。

</fragment>

    ```bash
    read -r -p "Username: " MC_USER
    read -r -s -p "Password: " MC_PASS; echo

    kubectl create secret generic mission-control-auth \
      --namespace=langsmith \
      --from-literal=username="$MC_USER" \
      --from-literal=password="$MC_PASS" \
      --dry-run=client -o yaml | kubectl apply -f -
    ```

对于多副本后端部署，请在同一 Secret 中包含一个共享的 JWT 签名密钥，并在 `values.yaml` 中设置 `config.auth.jwtSecretKey: jwtSecret`：

    ```bash
    JWT_SECRET="$(openssl rand -base64 32)"

    kubectl create secret generic mission-control-auth \
      --namespace=langsmith \
      --from-literal=username="$MC_USER" \
      --from-literal=password="$MC_PASS" \
      --from-literal=jwtSecret="$JWT_SECRET" \
      --dry-run=client -o yaml | kubectl apply -f -
    ```
</Step>
  <Step title="使用 Helm 安装">

</Step>
    ```bash
    helm upgrade --install mission-control langchain/mission-control \
      --namespace langsmith \
      --create-namespace \
      --values values.yaml \
      --rollback-on-failure
    ```

等待两个工作负载准备就绪（后端作为 StatefulSet 运行，前端作为 Deployment 运行）：

    ```bash
    kubectl rollout status statefulset/mission-control-backend -n langsmith
    kubectl rollout status deployment/mission-control-frontend -n langsmith
    ```

您也可以直接检查 Pod：

    ```bash
    kubectl get pods -n langsmith
    ```
</Step>
  <Step title="访问 UI">

</fragment>
    ```bash
    kubectl port-forward svc/mission-control-frontend 3000:3000 -n langsmith
    ```

<fragment>

    打开 [http://localhost:3000](http://localhost:3000/) 并使用步骤 3 中的凭据登录。
  </Step>
</Steps>

## 升级

下载最新的公共 values 文件，合并您需要的所有本地更改，然后运行：


</fragment>

```bash
helm repo update langchain

helm upgrade --install mission-control langchain/mission-control \
  --namespace langsmith \
  --values values.yaml \
  --rollback-on-failure
```

如果你正在使用本地 chart 检出版本的话：

```bash
helm upgrade --install mission-control . \
  --namespace langsmith \
  --values values.yaml \
  --rollback-on-failure
```

如果你使用快速脚本安装并保留在本地：

```bash
./install-script.sh install -f values.yaml
```

## 卸载

```bash
helm uninstall mission-control -n langsmith
```

这将删除 Mission Control 版本。它不会删除您的命名空间或无关的 LangSmith 资源。

可选清理 Mission Control 拥有的 Secrets：

```bash
kubectl delete secret -n langsmith \
  mission-control-auth \
  mission-control-draft \
  mission-control-deployed \
  mission-control-backup \
  mission-control-history \
  mission-control-alerts-config \
  mission-control-alerts-log \
  mission-control-alerts-key \
  mission-control-setup-token \
  --ignore-not-found
```

## 附加资源

### 故障排除

| 症状 | 检查项 |
|---------|---------------|
| `kubectl auth can-i ...` 返回 `no` | 请集群管理员授予安装时 RBAC 权限，或请其代为运行安装。 |
| Pod 一直处于 `Pending` 状态 | 检查 namespace 的 `ResourceQuota`、节点容量，以及使用 `kubectl describe pod -n langsmith <pod>` 查看 PVC/存储类事件。 |
| 镜像拉取错误 | 确认集群能够拉取 `langchain/mission-control-backend:latest` 和 `langchain/mission-control-frontend:latest`。 |
| 登录失败 | 确认 `mission-control-auth` 存在于同一 namespace 中，且包含 `username` 和 `password` 密钥。 |
| 浏览器无法连接 | 确认 port-forward 命令仍在运行，且没有其他本地进程占用端口 `3000`。 |

### 权限参考

Helm chart 会创建一个名为 `mission-control` 的 `ServiceAccount`、`ClusterRole` 和 `ClusterRoleBinding`。大多数权限为只读。写操作动词范围狭窄，由功能开关控制。

安装或升级需要创建集群级 RBAC（`ClusterRole` 和 `ClusterRoleBinding`）的权限，通常需要 `cluster-admin` 或自定义等效角色。最广泛的运行时权限集仅在 `config.features.deploy: true` 时使用；该标志默认启用，如需只读安装可将其设置为 `false`。

#### 始终存在的只读权限

| 资源组 | 资源 | 动词 |
|----------------|-----------|-------|
| 工作负载 | pods, pods/log, deployments, statefulsets, replicasets, daemonsets, jobs, cronjobs | get, list, watch |
| 网络 | services, endpoints, ingresses, ingressclasses | get, list, watch |
| 存储 | persistentvolumeclaims, storageclasses | get, list, watch |
| 集群 | nodes, namespaces, events, serviceaccounts, resourcequotas | get, list, watch |
| 配置 | configmaps, secrets | get, list |
| 指标 | metrics.k8s.io pods/nodes | get, list, watch |
| RBAC | roles, rolebindings, clusterroles, clusterrolebindings | get, list, watch |
| CRD 和扩展 | customresourcedefinitions, leases, scaledobjects, httproutes, virtualservices, lgps | get, list, watch |

#### 功能开关控制的权限

| 功能标志 | 资源 | 额外动词 |
|--------------|-----------|-------------|
| `config.features.configSave` | secrets (`mission-control-draft`) | create, update, delete |
| `config.features.alerts` | secrets (`mission-control-alerts-*`) | create, update, delete |
| `config.features.fixIssue` | pods | delete |
| `config.features.adopt` | secrets, configmaps, serviceaccounts, deployments, statefulsets | patch |
| `config.auth.enabled` | secrets (`mission-control-auth`, setup-token), backend statefulset | create, update, delete, patch |
| `config.features.valuesOverride` | secrets (`mission-control-values-overrides`) | create, update, delete |
| `config.features.deploy` | 工作负载、网络、RBAC、CRD、Helm release secrets | create, update, patch, delete |

在 `values.yaml` 中将功能标志设置为 `false` 可移除相应的写操作动词。禁用所有功能标志后，除 `config.auth.enabled: true` 时的身份验证设置权限外，Mission Control 基本为只读状态。
