# 标签

您可以通过向 `call()`/`run()`/`apply()` 方法传递 `tags` 参数来为您的回调函数添加标签。这对于筛选日志非常有用，例如，如果您想要记录所有发往特定 `LLMChain` 的请求，您可以添加一个标签，然后通过该标签筛选您的日志。您可以将标签传递给构造函数和请求回调函数，有关详细信息，请参见上面的示例。然后这些标签会传递给“start”回调方法的 `tags` 参数，即 `on_llm_start`、`on_chat_model_start`、`on_chain_start`、`on_tool_start`。
